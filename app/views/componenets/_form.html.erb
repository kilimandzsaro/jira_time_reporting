<%= form_for(componenet) do |f| %>
  <% if componenet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(componenet.errors.count, "error") %> prohibited this componenet from being saved:</h2>

      <ul>
      <% componenet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
