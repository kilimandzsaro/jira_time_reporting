<div class="field" id="vacation">
  <%= f.label :vacations %>
  <div class="input_group" %>
    <%= f.collection_select :id, Employee.all, :id, :display_name, placeholder: "", class: 'form-control', optional: true %>
    <%= f.date_field :start_date, autocomplete: "off", placeholder: "YYYY-MM-DD", class: 'form-control', optional: true %>
    <%= f.date_field :end_date, autocomplete: "off", placeholder: "YYYY-MM-DD", class: 'form-control', optional: true %>
    <%= f.hidden_field :report_ids, value: @report.id %>
    <%= button_to 'Add', vacations_path, method: :post, class: 'btn btn-info btn-xs' %>
  </div>
</div>